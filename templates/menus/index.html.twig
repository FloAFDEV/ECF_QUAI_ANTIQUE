{% extends 'base.html.twig' %}

{% block title %}Menus
{% endblock %}

{% block body %}
	<section style="background: #232526">
		<div class="tab-content text-white d-flex align-items-center flex-column text-start justify-content-between border-bottom">
			<div class="container-fluid">
				<div class="container-xxl py-5 mt-5 w-auto">
					<div class="container">
						<h1 class="mb-5 text-center text-white fadeInUp" data-wow-delay="1s">Découvrez nos Menus
							<span class="text-warning">Antique</span>
						</h1>
					</div>
					<div class="row mb-5 text-center">
						{% for menu in menus %}
							<div class="col-md-6">
								<h2 class="text-warning">{{ menu.title }}</h2>
								<ul class="list-unstyled">
									{% for category in menu.categories %}
										{% for dish in category.dishes %}
											<li>
												<p class="text-white">{{ dish.title }}
													-
													<span class="text-warning">{{ dish.price }}0
														<span class="text-white">€</span>
													</span>
												</p>
											</li>
										{% endfor %}
									{% endfor %}
								</ul>
							</div>
						{% endfor %}
					</div>
				</div>
			</div>
		</div>
		<div class="tab-content text-white col-lg-5 d-flex align-items-center w-100 flex-column text-start ps-4 justify-content-between border-bottom">
			<div class="container">
				<h1 class="mb-5 mt-5 text-center text-white fadeInUp" data-wow-delay="1s">Découvrez notre Carte
					<span class="text-warning">Antique</span>
				</h1>
			</div>
			<div class="justify-content-center border-bottom ">
				<span class="text-start text-white">
					{% for categorie in categories %}
					</span>
					<span class="text-warning justify-content-center border-bottom">
						<span>{{ categorie.title }}
						</span>
					</span>
					<div class="justify-content-center">
						<ul class="list-unstyled">
							{% for dishe in categorie.dishes %}
								<li>
									<span class="text-start text-white">
										-
										{{ dishe.title }}
										<span class="text-start text-warning">:
											{{ dishe.price }}0
											<span class="text-white">€
											</span>
										</span>
									</li>
								{% endfor %}
							</ul>
						{% endfor %}
					</div>
				</div>
			</div>
		</section>

	{% endblock %}

	{% block javascripts %}
		{{ parent() }}
		<script>
			$(document).ready(function () {
$('.nav-tabs a').click(function () {
$(this).tab('show');
});
});
		</script>
	{% endblock %}

